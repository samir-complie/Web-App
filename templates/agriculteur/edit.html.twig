<!DOCTYPE html>

<title>Edit Agriculteur</title>

{% block body %}
    <h1>Edit Agriculteur</h1>

    {{ include('agriculteur/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_agriculteur_index') }}">back to list</a>

    {{ include('agriculteur/_delete_form.html.twig') }}
{% endblock %}
