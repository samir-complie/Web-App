<form method="post" action="{{ path('app_livraison_delete', {'id': livraison.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette livraison ?');" class="d-inline">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ livraison.id) }}">
    <button type="submit" class="btn btn-danger">Supprimer</button>
</form>
