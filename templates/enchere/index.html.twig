{% extends 'base_front.html.twig' %}

{% block title %}Hello FrontOffice/ProductSingleController!{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1>Enchere Index</h1>

        <table class="table table-striped table-bordered">
            <thead class="thead-dark">
                <tr>
                    <th>Id</th>
                    <th>Dernière Prix</th>
                    <th>Date</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
            {% for enchere in encheres %}
                <tr>
                    <td>{{ enchere.id }}</td>
                    <td>{{ enchere.dernierePrix }}</td>
                    <td>{{ enchere.date ? enchere.date|date('Y-m-d H:i:s') : '' }}</td>
                    <td>
                        <a href="{{ path('app_enchere_show', {'id': enchere.id}) }}" class="btn btn-info btn-sm">Show</a>
                        <a href="{{ path('app_enchere_edit', {'id': enchere.id}) }}" class="btn btn-warning btn-sm">Edit</a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="4" class="text-center">No records found</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        <a href="{{ path('app_enchere_new') }}" class="btn btn-success">Create New</a>
    </div>
{% endblock %}
