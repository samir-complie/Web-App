{% extends 'base_front.html.twig' %}

{% block title %}Créer un nouveau Transporteur{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="mb-4">Créer un nouveau Transporteur</h1>

    <div class="card bg-light p-4 rounded shadow-sm">
            {{ include('transporteur/_form.html.twig') }}
    </div>

    <div class="mt-3">
            <a href="{{ path('app_transporteur_index') }}" class="btn btn-secondary">
                <i class="fas fa-arrow-left"></i> Retour à la liste
            </a>

            <a href="{{ path('app_transporteur_new') }}" class="btn btn-success">
                <i class="fas fa-plus"></i> Créer un nouveau Transporteur
            </a>
    </div>
    </div>

{% endblock %}
